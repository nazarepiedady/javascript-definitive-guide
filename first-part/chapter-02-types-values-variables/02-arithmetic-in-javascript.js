// addition
var sum = 1 + 1;

// subtration
var sub = 2 - 1;

// multiplication
var mul = 3 * 2;

// division
var div = 6 / 2;

// module
var mod = 8 % 2;

// every results
var results = [ sum, sub, mul, div, mod ];

// print each result
results.forEach(function show(result) {
  console.log(result);
});
